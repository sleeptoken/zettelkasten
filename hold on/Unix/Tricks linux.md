? Is a wild card in Unix
If you search `/?s?/` the shell will search for `/usr/` in the current directory 


Unshadow command is used to merge contents of 2 files eg. 
`sudo unshadow /etc/passwd /etc/shadow >> creds.txt`